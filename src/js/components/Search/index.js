import { connect } from 'react-redux';
import Search from './Search';

function mapStoreToProps(store) {
  return {
    //data to pass to props here
  };
}

export default connect(mapStoreToProps)(Search);